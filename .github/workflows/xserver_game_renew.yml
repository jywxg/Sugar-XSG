name: XServer-Game-Renew
on:
  workflow_dispatch:
  schedule:
  - cron: "0 19,7 * * *"  # UTC 时间，对应北京时间 03:00 / 15:00

permissions:
  actions: write
  contents: write  # 允许工作流向仓库提交和推送代码

jobs:
  renew:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 📥 拉取仓库
        uses: actions/checkout@v4

      - name: 🐍 安装 Python 依赖
        run: pip install requests --quiet

      - name: ⚙ 设置代理 (sing-box)
        # 代理如果全部失效会 exit 1，此设置允许无视报错继续执行，从而触发 Python 的直连降级
        continue-on-error: true
        env:
          NODE_LINK: ${{ secrets.NODE_LINK }}
        run: |
          bash setup_proxy.sh

      - name: 🔧 执行续期
        env:
          XSERVER_GAME_ACCOUNT: ${{ secrets.XSERVER_GAME_ACCOUNT }}
          NODE_LINK: ${{ secrets.NODE_LINK }}
          TG_BOT: ${{ secrets.TG_BOT }}
          # 关键修复：将 setup_proxy.sh 导出的 IS_PROXY 赋值给 Python 需要的 USE_PROXY
          USE_PROXY: ${{ env.IS_PROXY }}
        run: python3 xserver_game_renew.py

      - name: ⏱️ 更新运行记录
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ ! -f time.txt ]; then
            echo "首次生成时间：$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          else
            echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          fi

          git add time.txt

          if git diff --cached --quiet; then
            echo "无变化，无需提交"
          else
            git commit -m "⏱️ 更新时间文件: $(date +'%Y-%m-%d %H:%M:%S')"
            # 优化：使用动态分支名代替硬编码的 main，防止分支名不一致导致报错
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: 🧹 清理旧工作流运行记录
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "开始清理旧的 GitHub Actions 运行记录..."
          gh api --paginate \
            -H "Accept: application/vnd.github+json" \
            "/repos/${{ github.repository }}/actions/runs?per_page=100" \
            --jq '.workflow_runs[] | select(.status=="completed") | .id' | while read id
          do
            echo "删除运行记录: $id"
            gh api --method DELETE \
              -H "Accept: application/vnd.github+json" \
              "/repos/${{ github.repository }}/actions/runs/$id" || true
          done
          echo "清理完成"
